<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Anna Leigh Oddities</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Home", "/", class:"nav-link"%>
      </li>
      <li class="nav-item">
        <%= render partial: "/cart/cart_fragment", class: "nav_item"%>

      </li>
      <li class="nav-item">

        <% if user_signed_in? %>
          | Logged in as <%= current_user.email %> |
          <%= link_to "Account", profile_edit_path , class:"nav-item"%> |
          <%= link_to "Log out", destroy_user_session_path, :method=>'delete', class:"nav-item"%>
        <% else %>
          |  <%= link_to "Login", new_user_session_path, class:"nav-item" %>
        <% end %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Categories
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% @categories.each do |category| %>
          <%= link_to category.name, categories_show_path(:id => category.id), class:"dropdown-item"%>
            <div class="dropdown-divider"></div>
          <% end %>
        </div>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>